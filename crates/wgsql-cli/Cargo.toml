[package]
name = "wgsql-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "CLI for wgsql"

[[bin]]
name = "wgsql"
path = "src/main.rs"

[dependencies]
wgsql = { path = "../wgsql" }
anyhow = { workspace = true }
